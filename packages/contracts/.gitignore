# Foundry build artifacts
out/

# Foundry build cache
cache/

# Foundry deployment logs
broadcast/

# Circom build outputs (regenerated by: pnpm circuit:build)
circuits/build/

# BUT keep the compiled WASM needed for ZK proof generation
!circuits/build/DecisionCircuit_js/
!circuits/build/DecisionCircuit_js/DecisionCircuit.wasm

# ZK ceremony files (regenerated by: pnpm circuit:setup / circuit:contribute)
# .ptau files can be downloaded from standard trusted setup ceremonies
circuits/*.ptau
circuits/*.zkey

# BUT keep the final zkey needed for ZK proof generation
!circuits/circuit_final.zkey

# Environment files
.env
.env.*
